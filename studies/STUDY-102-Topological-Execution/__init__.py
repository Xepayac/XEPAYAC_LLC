"""
Lab Executable: Graph as Running Program

Simplest possible proof: arithmetic operations defined by graph structure.

The graph says "add 3 + 5" → the executor reads graph → result is 8.
Change the graph to "add 7 + 2" → result is 9.

The Python code doesn't know what numbers to add. The graph does.
"""

__version__ = "0.1.0"
